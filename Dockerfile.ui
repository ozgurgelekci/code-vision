# Railway Dockerfile for CodeVision.UI
FROM mcr.microsoft.com/dotnet/aspnet:9.0 AS base
WORKDIR /app
EXPOSE 8080

FROM mcr.microsoft.com/dotnet/sdk:9.0 AS build
WORKDIR /src

# Copy solution file
COPY ["CodeVision.sln", "."]

# Copy project files
COPY ["CodeVision.UI/CodeVision.UI.csproj", "CodeVision.UI/"]
COPY ["CodeVision.API/CodeVision.API.csproj", "CodeVision.API/"]
COPY ["CodeVision.Core/CodeVision.Core.csproj", "CodeVision.Core/"]
COPY ["CodeVision.Infrastructure/CodeVision.Infrastructure.csproj", "CodeVision.Infrastructure/"]

# Restore all projects
RUN dotnet restore "CodeVision.sln"

# Copy source code
COPY . .

# Build and publish UI
WORKDIR "/src/CodeVision.UI"
RUN dotnet tool install -g Microsoft.Web.LibraryManager.Cli
ENV PATH="$PATH:/root/.dotnet/tools"
RUN libman restore
RUN dotnet publish "CodeVision.UI.csproj" -c Release -o /app/publish

# Final stage
FROM base AS final
WORKDIR /app
COPY --from=build /app/publish .

# Railway configuration
ENV ASPNETCORE_ENVIRONMENT=Production

ENTRYPOINT ["dotnet", "CodeVision.UI.dll"]
